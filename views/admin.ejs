<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>D.E.T System | Master Node Console</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    
    <style>
        :root {
            --det-green: #B0BF96;
            --det-green-hover: #96a67c;
            --det-dark: #0a0a0a;
            --det-card: #141414;
            --det-card-hover: #1a1a1a;
            --det-border: rgba(176, 191, 150, 0.15);
            --det-border-active: rgba(176, 191, 150, 0.35);
            --det-red: #ef4444;
            --det-amber: #f59e0b;
            --det-blue: #38bdf8;
            --det-purple: #a78bfa;
            --det-emerald: #10b981;
            --sidebar-width: 280px;
            --transition-speed: 0.25s;
        }

        [data-bs-theme="light"] {
            --det-dark: #f8fafc;
            --det-card: #ffffff;
            --det-card-hover: #f1f5f9;
            --det-border: rgba(0, 0, 0, 0.1);
            --det-border-active: rgba(0, 0, 0, 0.2);
        }

        * { scrollbar-width: thin; scrollbar-color: var(--det-border) transparent; }
        *::-webkit-scrollbar { width: 6px; height: 6px; }
        *::-webkit-scrollbar-thumb { background: var(--det-border); border-radius: 3px; }
        *::-webkit-scrollbar-thumb:hover { background: var(--det-green); }

        body { 
            background-color: var(--det-dark); 
            color: #e2e8f0; 
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif; 
            overflow-x: hidden; 
            margin: 0;
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
            font-size: 15px;
        }
        [data-bs-theme="light"] body { color: #0f172a; }

        /* =========================================
           PREMIUM SIDEBAR
        ========================================= */
        .sidebar {
            position: fixed; top: 0; left: 0; height: 100vh; width: var(--sidebar-width);
            background: linear-gradient(180deg, #0f0f0f 0%, #0a0a0a 100%);
            border-right: 1px solid var(--det-border);
            z-index: 1050; display: flex; flex-direction: column; 
            transition: transform var(--transition-speed) cubic-bezier(0.4, 0, 0.2, 1);
            backdrop-filter: blur(10px);
        }
        [data-bs-theme="light"] .sidebar { background: linear-gradient(180deg, #ffffff 0%, #f8fafc 100%); }
        .sidebar-header { 
            padding: 28px 24px; 
            border-bottom: 1px solid var(--det-border);
            background: linear-gradient(135deg, rgba(176,191,150,0.05) 0%, transparent 100%);
        }
        .text-logo { 
            color: var(--det-green) !important; 
            font-size: 24px; 
            font-weight: 800; 
            text-decoration: none; 
            letter-spacing: -0.5px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .text-logo span { 
            color: #ffffff !important; 
            font-weight: 400;
            font-size: 16px;
            background: rgba(176,191,150,0.1);
            padding: 4px 12px;
            border-radius: 6px;
            letter-spacing: 2px;
        }
        [data-bs-theme="light"] .text-logo span { color: #0f172a !important; }
        
        .sidebar-nav { 
            padding: 20px 12px; 
            flex-grow: 1; 
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 2px;
        }
        .nav-section {
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: 700;
            color: #64748b !important;
            padding: 16px 16px 8px;
            margin-top: 8px;
        }
        .nav-section:first-child { margin-top: 0; }
        
        .nav-link {
            display: flex; align-items: center; padding: 12px 16px; 
            color: #94a3b8 !important;
            text-decoration: none; border-radius: 10px; font-weight: 500; 
            font-size: 14px; transition: all var(--transition-speed); position: relative;
            margin: 1px 0;
        }
        .nav-link i { margin-right: 12px; font-size: 18px; width: 20px; text-align: center; opacity: 0.7; transition: all var(--transition-speed); }
        .nav-link:hover { 
            color: #fff !important; 
            background: rgba(255,255,255,0.04); 
            transform: translateX(2px);
        }
        [data-bs-theme="light"] .nav-link:hover { color: #0f172a !important; }
        .nav-link:hover i { opacity: 1; }
        .nav-link.active { 
            color: var(--det-dark) !important; 
            background: linear-gradient(135deg, var(--det-green) 0%, var(--det-green-hover) 100%);
            font-weight: 600; 
            box-shadow: 0 4px 12px rgba(176,191,150,0.25);
        }
        .nav-link.active i { opacity: 1; color: var(--det-dark); }
        .nav-link .badge { 
            font-size: 10px; 
            padding: 3px 8px; 
            border-radius: 12px;
            font-weight: 700;
            letter-spacing: 0.5px;
        }
        .badge-beta { background: rgba(245,158,11,0.15); color: #f59e0b; }
        .badge-locked { background: rgba(100,116,139,0.2); color: #94a3b8; }

        .sidebar-footer { 
            padding: 20px 24px; 
            border-top: 1px solid var(--det-border); 
            font-size: 13px; 
            color: #64748b;
            background: rgba(0,0,0,0.3);
        }
        [data-bs-theme="light"] .sidebar-footer { background: rgba(0,0,0,0.03); }

        /* =========================================
           MAIN CONTENT AREA
        ========================================= */
        .main-content { 
            margin-left: var(--sidebar-width); 
            min-height: 100vh; 
            display: flex; 
            flex-direction: column; 
            transition: margin-left var(--transition-speed);
            background: radial-gradient(ellipse at top right, rgba(176,191,150,0.03) 0%, transparent 60%);
        }

        .top-bar {
            background: rgba(10,10,10,0.8); 
            backdrop-filter: blur(20px);
            padding: 16px 32px; 
            border-bottom: 1px solid var(--det-border);
            display: flex; justify-content: space-between; align-items: center; 
            position: sticky; top: 0; z-index: 1040;
        }
        [data-bs-theme="light"] .top-bar { background: rgba(255,255,255,0.8); }
        .mobile-toggle { display: none; color: var(--det-green); font-size: 24px; cursor: pointer; background: none; border: none; padding: 8px; border-radius: 8px; transition: all var(--transition-speed); }
        .mobile-toggle:hover { background: rgba(176,191,150,0.1); }
        
        .nav-quick-btn {
            background: var(--det-card); border: 1px solid var(--det-border); 
            color: #94a3b8; padding: 8px 18px; border-radius: 10px; 
            font-size: 13px; font-weight: 500; text-decoration: none; 
            transition: all var(--transition-speed); display: inline-flex; align-items: center; gap: 7px;
        }
        .nav-quick-btn:hover { 
            background: rgba(176,191,150,0.1); 
            color: var(--det-green); 
            border-color: var(--det-border-active); 
            transform: translateY(-1px);
        }

        .dashboard-wrapper { padding: 32px; max-width: 1800px; width: 100%; margin: 0 auto; }
        
        /* =========================================
           GLASS CARDS & KPI
        ========================================= */
        .glass-card { 
            background: var(--det-card); 
            border: 1px solid var(--det-border); 
            border-radius: 16px; 
            box-shadow: 0 4px 24px rgba(0,0,0,0.3); 
            padding: 28px; 
            height: 100%;
            transition: all var(--transition-speed);
            position: relative;
            overflow: hidden;
        }
        [data-bs-theme="light"] .glass-card { box-shadow: 0 4px 24px rgba(0,0,0,0.05); }
        .glass-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(176,191,150,0.1), transparent);
        }
        .glass-card:hover {
            border-color: var(--det-border-active);
            box-shadow: 0 8px 32px rgba(0,0,0,0.4);
        }

        .section-heading {
            display: flex; align-items: center; justify-content: space-between;
            margin: 40px 0 20px;
        }
        .section-heading:first-of-type { margin-top: 0; }
        .section-heading h6 {
            font-size: 12px; text-transform: uppercase; letter-spacing: 2px; font-weight: 700;
            color: #64748b !important; margin: 0; display: flex; align-items: center; gap: 10px;
        }
        .section-heading h6 i { color: var(--det-green); font-size: 16px; }

        /* KPI Cards */
        .kpi-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 16px;
            margin-bottom: 32px;
        }
        .kpi-card {
            background: var(--det-card); 
            border: 1px solid var(--det-border); 
            border-radius: 16px;
            padding: 24px; 
            display: flex; 
            align-items: center; 
            gap: 16px; 
            transition: all var(--transition-speed);
            cursor: default;
        }
        .kpi-card:hover { 
            transform: translateY(-2px); 
            border-color: var(--det-border-active);
            box-shadow: 0 12px 40px rgba(0,0,0,0.3);
        }
        .kpi-icon {
            width: 52px; height: 52px; min-width: 52px; border-radius: 14px;
            display: flex; align-items: center; justify-content: center; font-size: 22px;
        }
        .kpi-content { flex: 1; min-width: 0; }
        .kpi-label { font-size: 12px; text-transform: uppercase; letter-spacing: 1px; color: #64748b !important; font-weight: 600; margin-bottom: 4px; }
        .kpi-value { font-size: 28px; font-weight: 800; color: #fff !important; line-height: 1.1; margin-bottom: 2px; }
        [data-bs-theme="light"] .kpi-value { color: #0f172a !important; }
        .kpi-sub { font-size: 12px; color: #64748b !important; }

        /* =========================================
           TABLES & PAGINATION
        ========================================= */
        .table-container {
            border-radius: 12px;
            border: 1px solid var(--det-border);
            overflow: hidden;
            background: var(--det-dark);
        }
        .table-responsive {
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
        }
        .table {
            margin-bottom: 0;
            color: #e2e8f0 !important;
            min-width: 900px;
            border-collapse: separate;
            border-spacing: 0;
            font-size: 15px;
        }
        [data-bs-theme="light"] .table { color: #0f172a !important; }
        .table thead th { 
            background: #0a0a0a; 
            color: #94a3b8 !important; 
            font-weight: 600; 
            font-size: 12px; 
            text-transform: uppercase; 
            letter-spacing: 1.5px; 
            border-bottom: 2px solid var(--det-border); 
            padding: 16px;
            white-space: nowrap;
            position: sticky;
            top: 0;
            z-index: 1;
        }
        [data-bs-theme="light"] .table thead th { background: #f1f5f9; }
        .table tbody td { 
            padding: 16px; 
            border-bottom: 1px solid rgba(255,255,255,0.03); 
            vertical-align: middle; 
            background-color: transparent; 
            color: #e2e8f0 !important;
            font-size: 15px;
        }
        [data-bs-theme="light"] .table tbody td { color: #0f172a !important; }
        .table tbody tr { transition: all var(--transition-speed); }
        .table tbody tr:hover td { background-color: rgba(176, 191, 150, 0.03); }
        .table tbody tr.row-hidden { display: none; }

        /* Pagination */
        .pagination-container {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 16px 20px;
            background: #0a0a0a;
            border-top: 1px solid var(--det-border);
            flex-wrap: wrap;
            gap: 12px;
        }
        [data-bs-theme="light"] .pagination-container { background: #f1f5f9; }
        .pagination-info {
            font-size: 14px;
            color: #94a3b8;
        }
        .pagination-controls {
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .pagination {
            display: flex;
            gap: 4px;
            margin: 0;
            list-style: none;
            padding: 0;
        }
        .page-btn {
            width: 36px;
            height: 36px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 8px;
            border: 1px solid var(--det-border);
            background: var(--det-card);
            color: #94a3b8;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: all var(--transition-speed);
            user-select: none;
        }
        .page-btn:hover:not(:disabled):not(.active) {
            border-color: var(--det-green);
            color: var(--det-green);
            background: rgba(176,191,150,0.05);
        }
        .page-btn.active {
            background: var(--det-green);
            color: var(--det-dark);
            border-color: var(--det-green);
            font-weight: 700;
        }
        .page-btn:disabled {
            opacity: 0.3;
            cursor: not-allowed;
        }
        .page-size-select {
            background: var(--det-card);
            border: 1px solid var(--det-border);
            color: #94a3b8;
            padding: 8px 12px;
            border-radius: 8px;
            font-size: 14px;
            cursor: pointer;
            outline: none;
        }
        .page-size-select:focus {
            border-color: var(--det-green);
        }

        /* Status Badges */
        .status-badge {
            padding: 6px 14px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            white-space: nowrap;
            display: inline-flex;
            align-items: center;
            gap: 6px;
        }
        .status-dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            display: inline-block;
        }
        .bg-det-green { background: rgba(176, 191, 150, 0.12); color: var(--det-green) !important; border: 1px solid rgba(176, 191, 150, 0.25); }
        .bg-det-red { background: rgba(239, 68, 68, 0.12); color: #ef4444 !important; border: 1px solid rgba(239, 68, 68, 0.25); }
        .bg-det-warn { background: rgba(245, 158, 11, 0.12); color: #f59e0b !important; border: 1px solid rgba(245, 158, 11, 0.25); }
        .bg-det-blue { background: rgba(56, 189, 248, 0.12); color: var(--det-blue) !important; border: 1px solid rgba(56, 189, 248, 0.25); }
        .bg-det-slate { background: rgba(100, 116, 139, 0.15); color: #94a3b8 !important; border: 1px solid rgba(100, 116, 139, 0.25); }

        .code-pill { 
            background: #0a0a0a; 
            border: 1px solid var(--det-border); 
            color: var(--det-green) !important; 
            padding: 5px 12px; 
            border-radius: 8px; 
            font-family: 'SF Mono', 'Fira Code', monospace; 
            font-size: 13px; 
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        /* Action Buttons */
        .btn-det { 
            background: linear-gradient(135deg, var(--det-green) 0%, var(--det-green-hover) 100%);
            color: var(--det-dark) !important; 
            font-weight: 600; 
            transition: all var(--transition-speed); 
            border: none;
            border-radius: 10px;
            padding: 10px 20px;
            font-size: 14px;
        }
        .btn-det:hover { 
            transform: translateY(-1px);
            box-shadow: 0 6px 20px rgba(176,191,150,0.3);
            color: #000 !important; 
        }
        
        .action-group {
            display: flex;
            gap: 6px;
            justify-content: flex-end;
        }
        .action-btn {
            width: 38px; 
            height: 38px; 
            border-radius: 10px; 
            border: 1px solid var(--det-border); 
            background: var(--det-card);
            color: #94a3b8; 
            display: inline-flex; 
            align-items: center; 
            justify-content: center; 
            transition: all var(--transition-speed); 
            cursor: pointer;
            font-size: 15px;
        }
        .action-btn:hover { transform: translateY(-1px); }
        .btn-manage:hover { border-color: var(--det-green); color: var(--det-green); background: rgba(176,191,150,0.08); }
        .btn-ban:hover { border-color: var(--det-amber); color: var(--det-amber); background: rgba(245,158,11,0.08); }
        .btn-delete:hover { border-color: var(--det-red); color: var(--det-red); background: rgba(239,68,68,0.08); }
        .btn-details:hover { border-color: var(--det-blue); color: var(--det-blue); background: rgba(56,189,248,0.08); }

        /* Terminal indicator */
        .terminal-active { color: var(--det-emerald) !important; }
        .terminal-inactive { color: #64748b !important; }

        /* Search */
        .search-box {
            position: relative;
            min-width: 280px;
        }
        .search-box i {
            position: absolute;
            left: 14px;
            top: 50%;
            transform: translateY(-50%);
            color: #64748b;
            font-size: 16px;
        }
        .search-input {
            background: #0a0a0a !important;
            border: 1px solid var(--det-border) !important;
            color: #e2e8f0 !important;
            padding: 10px 16px 10px 42px;
            border-radius: 10px;
            font-size: 15px;
            width: 100%;
            transition: all var(--transition-speed);
        }
        .search-input:focus {
            border-color: var(--det-green) !important;
            box-shadow: 0 0 0 3px rgba(176,191,150,0.1) !important;
            outline: none;
        }
        .search-input::placeholder { color: #64748b; }

        /* Empty states */
        .empty-state {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 48px 24px;
            color: #64748b;
        }
        .empty-state i { 
            font-size: 40px; 
            color: var(--det-border); 
            margin-bottom: 16px;
            opacity: 0.5;
        }
        .empty-state strong { color: #94a3b8; font-size: 16px; display: block; margin-bottom: 6px; }
        .empty-state span { font-size: 14px; max-width: 360px; line-height: 1.5; }

        /* Mini rows */
        .mini-row {
            display: flex; align-items: center; justify-content: space-between; gap: 12px;
            padding: 14px 16px; border-radius: 10px; background: #0a0a0a;
            border: 1px solid rgba(255,255,255,0.03); margin-bottom: 8px;
            transition: all var(--transition-speed);
        }
        [data-bs-theme="light"] .mini-row { background: #f1f5f9; border-color: rgba(0,0,0,0.05); }
        .mini-row:hover { border-color: var(--det-border); background: var(--det-card); }
        .mini-row:last-child { margin-bottom: 0; }
        .rank-badge {
            width: 32px; height: 32px; border-radius: 8px; display: flex; align-items: center; justify-content: center;
            font-size: 13px; font-weight: 800; background: rgba(176,191,150,0.1); color: var(--det-green);
            flex-shrink: 0;
        }
        .rank-badge.rank-1 { background: linear-gradient(135deg, #f59e0b, #d97706); color: #000; }
        .rank-badge.rank-2 { background: linear-gradient(135deg, #94a3b8, #64748b); color: #000; }
        .rank-badge.rank-3 { background: linear-gradient(135deg, #cd853f, #8b6914); color: #000; }

        /* Accordion */
        .accordion-button {
            background: var(--det-card) !important;
            color: #fff !important;
            border: 1px solid var(--det-border) !important;
            border-radius: 10px !important;
            font-weight: 600;
            font-size: 15px;
            padding: 14px 20px;
        }
        [data-bs-theme="light"] .accordion-button { color: #0f172a !important; background: #ffffff !important; }
        .accordion-button:not(.collapsed) {
            background: rgba(176,191,150,0.08) !important;
            border-color: var(--det-border-active) !important;
            box-shadow: none;
        }
        .accordion-button::after { filter: invert(0.5); }
        .accordion-body { background: var(--det-dark); border-radius: 0 0 10px 10px; padding: 16px; }

        /* Modal */
        .modal-content { 
            background-color: var(--det-card) !important; 
            color: #e2e8f0 !important; 
            border: 1px solid var(--det-border); 
            border-radius: 16px; 
            box-shadow: 0 24px 80px rgba(0,0,0,0.6);
        }
        [data-bs-theme="light"] .modal-content { color: #0f172a !important; }
        .modal-header { border-bottom: 1px solid rgba(255,255,255,0.05); padding: 24px 28px; }
        .modal-footer { border-top: 1px solid rgba(255,255,255,0.05); padding: 20px 28px; }
        .modal-body { padding: 24px 28px; }
        .form-control, .form-select { 
            background-color: #0a0a0a !important; 
            border: 1px solid var(--det-border) !important; 
            color: #e2e8f0 !important; 
            padding: 12px 16px; 
            border-radius: 10px;
            font-size: 15px;
            transition: all var(--transition-speed);
        }
        .form-control:focus, .form-select:focus { 
            border-color: var(--det-green) !important; 
            box-shadow: 0 0 0 3px rgba(176, 191, 150, 0.1) !important; 
        }
        .btn-close-white { filter: invert(1) grayscale(100%) brightness(200%); }

        /* Detail rows */
        .detail-row {
            display: flex; justify-content: space-between; align-items: center;
            padding: 12px 0; border-bottom: 1px solid rgba(255,255,255,0.03);
        }
        .detail-row:last-child { border-bottom: none; }
        .detail-label { color: #64748b; font-size: 12px; text-transform: uppercase; letter-spacing: 1px; font-weight: 600; }
        .detail-value { color: #e2e8f0; font-weight: 600; font-size: 15px; text-align: right; }

        /* Charts */
        .chart-wrap { position: relative; height: 300px; }
        .chart-wrap.short { height: 240px; }

        /* Responsive */
        @media (max-width: 1199px) {
            .sidebar { transform: translateX(-100%); }
            .sidebar.show { transform: translateX(0); }
            .main-content { margin-left: 0; }
            .mobile-toggle { display: block; }
            .dashboard-wrapper { padding: 20px 16px; }
            .kpi-grid { grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); }
        }
        @media (max-width: 767px) {
            .kpi-grid { grid-template-columns: 1fr 1fr; gap: 10px; }
            .kpi-value { font-size: 24px; }
            .top-bar { padding: 12px 16px; }
            .nav-quick-btn { display: none; }
            .search-box { min-width: 100%; }
            .pagination-container { flex-direction: column; align-items: flex-start; }
        }

        .sidebar-backdrop {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.6);
            z-index: 1049;
            backdrop-filter: blur(2px);
        }
        .sidebar-backdrop.show { display: block; }

        #themeToggle, #logoutBtn {
            padding: 8px 18px;
            font-size: 14px;
            font-weight: 500;
            border-radius: 10px;
            transition: all var(--transition-speed);
        }
        #themeToggle {
            background: var(--det-card) !important;
            border: 1px solid var(--det-border) !important;
            color: #94a3b8 !important;
        }
        #themeToggle:hover { border-color: var(--det-green); color: var(--det-green); }
        #logoutBtn {
            background: rgba(239,68,68,0.1) !important;
            border: 1px solid rgba(239,68,68,0.3) !important;
            color: #ef4444 !important;
        }
        #logoutBtn:hover { background: rgba(239,68,68,0.2) !important; }
    </style>
</head>
<body>

    <nav class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <a href="/admin" class="text-logo">
                <i class="bi bi-cpu-fill"></i> D.E.T <span>ADMIN</span>
            </a>
        </div>
        <div class="sidebar-nav">
            <div class="nav-section">Core Systems</div>
            <a href="#overview" class="nav-link active"><i class="bi bi-grid-1x2-fill"></i> Master Console</a>
            <a href="#analytics" class="nav-link"><i class="bi bi-graph-up-arrow"></i> Network Analytics</a>
            <a href="#live-feed" class="nav-link"><i class="bi bi-broadcast"></i> Live S.M.A.R.T Feed</a>
            <a href="#logouts" class="nav-link"><i class="bi bi-shield-lock-fill"></i> Account Logouts</a>
            <a href="#top-earners" class="nav-link"><i class="bi bi-trophy-fill"></i> Top Earners</a>
            <a href="#ledger" class="nav-link"><i class="bi bi-people-fill"></i> Participant Ledger</a>

            <div class="nav-section">Network Engines v2.0</div>
            <a href="#affiliates" class="nav-link"><i class="bi bi-diagram-3-fill"></i> Affiliate Network <span class="badge badge-beta">BETA</span></a>
            <a href="#" class="nav-link"><i class="bi bi-globe2"></i> Geo-Block Config <span class="badge badge-locked">LOCKED</span></a>

            <div class="nav-section">Quick Navigate</div>
            <a href="/" class="nav-link" target="_blank"><i class="bi bi-house"></i> Public Homepage</a>
            <a href="/pricing" class="nav-link" target="_blank"><i class="bi bi-tags"></i> Pricing Page</a>
            <a href="/dashboard" class="nav-link" target="_blank"><i class="bi bi-speedometer2"></i> User Dashboard</a>

            <div class="nav-section">System</div>
            <a href="/logout" class="nav-link" style="color: #ef4444 !important;"><i class="bi bi-power"></i> Kill Session</a>
        </div>
        <div class="sidebar-footer d-flex justify-content-between align-items-center">
            <div><span class="spinner-grow spinner-grow-sm text-success me-2" style="width: 8px; height: 8px;"></span>Node Routing</div>
            <span style="color: var(--det-green);">v2.5.0</span>
        </div>
    </nav>

    <main class="main-content">
        
        <header class="top-bar">
            <div class="d-flex align-items-center gap-3">
                <button class="mobile-toggle" id="sidebarToggle"><i class="bi bi-list"></i></button>
                <div>
                    <h5 class="mb-0 fw-bold text-white d-none d-md-block" style="font-size: 18px;">Central Command Hub</h5>
                    <small class="d-none d-md-block" style="color: #64748b; font-size: 12px;">Real-time network monitoring & control</small>
                </div>
            </div>
            
            <div class="d-flex align-items-center gap-3">
                <button id="themeToggle"><i class="bi bi-sun-fill" id="themeIcon"></i></button>
                <a href="/logout" id="logoutBtn"><i class="bi bi-box-arrow-right me-1"></i> Logout</a>
                <a href="/" target="_blank" class="nav-quick-btn d-none d-lg-inline-flex">
                    <i class="bi bi-house"></i> <span>Site</span>
                </a>
                <a href="/pricing" target="_blank" class="nav-quick-btn d-none d-lg-inline-flex">
                    <i class="bi bi-tags"></i> <span>Pricing</span>
                </a>
                <a href="/dashboard" target="_blank" class="nav-quick-btn d-none d-lg-inline-flex">
                    <i class="bi bi-speedometer2"></i> <span>Dashboard</span>
                </a>
                <div class="vr d-none d-sm-block" style="height: 24px; background: var(--det-border);"></div>
                <div class="text-end d-none d-sm-block">
                    <small class="d-block" style="font-size: 11px; line-height: 1; color: #64748b; letter-spacing: 1px; text-transform: uppercase;">Administrator</small>
                    <span class="fw-bold" style="font-size: 15px; color: var(--det-green) !important;"><%= currentUser.username.toUpperCase() %></span>
                </div>
            </div>
        </header>

        <div class="dashboard-wrapper">
            
            <% if(success && success.length > 0) { %> 
                <div class="alert bg-success bg-opacity-10 border-success text-success fw-medium mb-4" style="border-radius: 10px; border-left: 3px solid #22c55e; font-size: 15px;">
                    <i class="bi bi-check-circle-fill me-2"></i><%= success %>
                </div> 
            <% } %>
            <% if(error && error.length > 0) { %> 
                <div class="alert bg-danger bg-opacity-10 border-danger text-danger fw-medium mb-4" style="border-radius: 10px; border-left: 3px solid #ef4444; font-size: 15px;">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i><%= error %>
                </div> 
            <% } %>

            <!-- KPI GRID -->
            <div id="overview"></div>
            <div class="kpi-grid">
                <div class="kpi-card">
                    <div class="kpi-icon" style="background: rgba(176,191,150,0.1); color: var(--det-green);">
                        <i class="bi bi-eye-fill"></i>
                    </div>
                    <div class="kpi-content">
                        <div class="kpi-label">Total Visitors</div>
                        <div class="kpi-value"><%= stats.totalVisitors.toLocaleString() %></div>
                        <div class="kpi-sub">All-time site visits</div>
                    </div>
                </div>
                <div class="kpi-card">
                    <div class="kpi-icon" style="background: rgba(56,189,248,0.1); color: var(--det-blue);">
                        <i class="bi bi-people-fill"></i>
                    </div>
                    <div class="kpi-content">
                        <div class="kpi-label">Total Accounts</div>
                        <div class="kpi-value"><%= stats.total %></div>
                        <div class="kpi-sub">Registered participants</div>
                    </div>
                </div>
                <div class="kpi-card">
                    <div class="kpi-icon" style="background: rgba(16,185,129,0.1); color: var(--det-emerald);">
                        <i class="bi bi-shield-check-fill"></i>
                    </div>
                    <div class="kpi-content">
                        <div class="kpi-label">Active Licenses</div>
                        <div class="kpi-value"><%= stats.active %></div>
                        <div class="kpi-sub"><%= stats.total > 0 ? Math.round((stats.active / stats.total) * 100) : 0 %>% of network</div>
                    </div>
                </div>
                <div class="kpi-card">
                    <div class="kpi-icon" style="background: rgba(239,68,68,0.1); color: var(--det-red);">
                        <i class="bi bi-shield-lock-fill"></i>
                    </div>
                    <div class="kpi-content">
                        <div class="kpi-label">Negative Logouts</div>
                        <div class="kpi-value"><%= stats.locked %></div>
                        <div class="kpi-sub">Zero-Hedge closures</div>
                    </div>
                </div>
                <div class="kpi-card">
                    <div class="kpi-icon" style="background: rgba(16,185,129,0.1); color: var(--det-emerald);">
                        <i class="bi bi-graph-up-arrow"></i>
                    </div>
                    <div class="kpi-content">
                        <div class="kpi-label">Positive Logouts</div>
                        <div class="kpi-value"><%= stats.locked %></div>
                        <div class="kpi-sub">100% gain achieved</div>
                    </div>
                </div>
                <div class="kpi-card">
                    <div class="kpi-icon" style="background: rgba(176,191,150,0.1); color: var(--det-green);">
                        <i class="bi bi-cash-stack"></i>
                    </div>
                    <div class="kpi-content">
                        <div class="kpi-label">Total Revenue</div>
                        <div class="kpi-value">$<%= stats.totalRevenue.toLocaleString() %></div>
                        <div class="kpi-sub">Payouts: $<%= stats.totalAffiliatePayouts.toLocaleString() %></div>
                    </div>
                </div>
                <div class="kpi-card">
                    <div class="kpi-icon" style="background: rgba(245,158,11,0.1); color: var(--det-amber);">
                        <i class="bi bi-hourglass-split"></i>
                    </div>
                    <div class="kpi-content">
                        <div class="kpi-label">Unlicensed / Pending</div>
                        <div class="kpi-value"><%= stats.unlicensed %></div>
                        <div class="kpi-sub"><%= stats.suspended %> banned · <%= stats.expired %> expired</div>
                    </div>
                </div>
            </div>

            <!-- ANALYTICS CHARTS -->
            <div id="analytics"></div>
            <div class="section-heading">
                <h6><i class="bi bi-graph-up-arrow"></i> Network Analytics</h6>
                <span style="font-size: 11px; color: #64748b; letter-spacing: 1px;">UPDATED LIVE</span>
            </div>

            <div class="row g-4 mb-4">
                <div class="col-xl-8">
                    <div class="glass-card">
                        <div class="d-flex justify-content-between align-items-start mb-4">
                            <div>
                                <h6 class="fw-bold mb-1 text-white" style="font-size: 16px;">Registrations — Last 30 Days</h6>
                                <p class="text-secondary small mb-0" style="font-size: 13px;">New accounts created per day, network-wide</p>
                            </div>
                            <span class="status-badge bg-det-green"><span class="status-dot" style="background: #10b981;"></span>Live</span>
                        </div>
                        <div class="chart-wrap">
                            <canvas id="registrationChart"></canvas>
                        </div>
                    </div>
                </div>
                <div class="col-xl-4">
                    <div class="glass-card">
                        <h6 class="fw-bold mb-1 text-white" style="font-size: 16px;">Account Status</h6>
                        <p class="text-secondary small mb-3" style="font-size: 13px;">Real-time breakdown</p>
                        <div class="chart-wrap short">
                            <canvas id="statusChart"></canvas>
                        </div>
                        <div class="mt-3">
                            <% 
                            const statusLabels = ['Active', 'Banned', 'Locked', 'Expired', 'Unlicensed'];
                            const statusValues = [stats.active, stats.suspended, stats.locked, stats.expired, stats.unlicensed];
                            const statusColors = ['#B0BF96', '#f59e0b', '#ef4444', '#94a3b8', '#38bdf8'];
                            for(let i = 0; i < statusLabels.length; i++) { %>
                                <div class="d-flex justify-content-between align-items-center mb-2">
                                    <span style="font-size: 13px; color: #94a3b8;"><span style="color: <%= statusColors[i] %>;">●</span> <%= statusLabels[i] %></span>
                                    <span class="fw-bold text-white" style="font-size: 15px;"><%= statusValues[i] %></span>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row g-4 mb-4">
                <div class="col-xl-6">
                    <div class="glass-card">
                        <h6 class="fw-bold mb-1 text-white" style="font-size: 16px;">Revenue by D.E.T Plan</h6>
                        <p class="text-secondary small mb-3" style="font-size: 13px;">Verified revenue per tier</p>
                        <div class="chart-wrap">
                            <canvas id="revenueChart"></canvas>
                        </div>
                    </div>
                </div>
                <div class="col-xl-6">
                    <div class="glass-card">
                        <h6 class="fw-bold mb-1 text-white" style="font-size: 16px;">Top Accounts by Region</h6>
                        <p class="text-secondary small mb-3" style="font-size: 13px;">Geographic distribution</p>
                        <div class="chart-wrap">
                            <canvas id="regionChart"></canvas>
                        </div>
                    </div>
                </div>
            </div>

            <!-- LIVE FEED + LOGOUTS -->
            <div id="live-feed"></div>
            <div id="logouts"></div>
            <div class="row g-4 mb-4">
                <div class="col-xl-8">
                    <div class="glass-card">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <div>
                                <h5 class="fw-bold m-0 text-white" style="font-size: 18px;"><i class="bi bi-broadcast me-2" style="color: var(--det-green);"></i>S.M.A.R.T Master Feed</h5>
                                <small style="color: #64748b; font-size: 13px;">Real-time trading signals</small>
                            </div>
                            <span class="status-badge bg-det-green">
                                <span class="spinner-grow spinner-grow-sm me-1" style="width: 6px; height: 6px;"></span>Transmitting
                            </span>
                        </div>

                        <div class="row g-3 mb-4">
                            <div class="col-md-3 col-6">
                                <div style="background: #0a0a0a; border: 1px solid rgba(255,255,255,0.05); border-radius: 10px; padding: 16px;">
                                    <small style="color: #64748b; font-size: 11px; text-transform: uppercase; letter-spacing: 1px;">Active Asset</small>
                                    <h5 id="ea-symbol" class="fw-bold text-white mt-1 mb-0" style="font-size: 20px;">--</h5>
                                </div>
                            </div>
                            <div class="col-md-3 col-6">
                                <div style="background: #0a0a0a; border: 1px solid rgba(255,255,255,0.05); border-radius: 10px; padding: 16px;">
                                    <small style="color: #64748b; font-size: 11px; text-transform: uppercase; letter-spacing: 1px;">Live Price</small>
                                    <h5 id="ea-price" class="fw-bold mt-1 mb-0" style="font-size: 20px; color: var(--det-green);">0.00</h5>
                                </div>
                            </div>
                            <div class="col-md-3 col-6">
                                <div style="background: #0a0a0a; border: 1px solid rgba(255,255,255,0.05); border-radius: 10px; padding: 16px;">
                                    <small style="color: #64748b; font-size: 11px; text-transform: uppercase; letter-spacing: 1px;">A.I. Trend</small>
                                    <h5 id="ea-trend" class="fw-bold mt-1 mb-0 text-white" style="font-size: 20px;">--</h5>
                                </div>
                            </div>
                            <div class="col-md-3 col-6">
                                <div style="background: #0a0a0a; border: 1px solid rgba(255,255,255,0.05); border-radius: 10px; padding: 16px;">
                                    <small style="color: #64748b; font-size: 11px; text-transform: uppercase; letter-spacing: 1px;">Open Trades</small>
                                    <h5 id="ea-trades" class="text-white fw-bold mt-1 mb-0" style="font-size: 20px;">0</h5>
                                </div>
                            </div>
                        </div>

                        <div>
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <small style="color: #64748b; font-size: 11px; text-transform: uppercase; letter-spacing: 1px;">Live Equity Curve</small>
                                <select id="equityPeriod" style="background: #0a0a0a; border: 1px solid var(--det-border); color: #94a3b8; padding: 6px 12px; border-radius: 8px; font-size: 12px;">
                                    <option value="1h">1 Hour</option>
                                    <option value="1d" selected>1 Day</option>
                                    <option value="1w">1 Week</option>
                                    <option value="1M">1 Month</option>
                                    <option value="1y">1 Year</option>
                                </select>
                            </div>
                            <div style="background: #0a0a0a; border: 1px solid rgba(255,255,255,0.05); border-radius: 10px; padding: 16px;">
                                <canvas id="equityChart" style="max-height: 220px;"></canvas>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-xl-4">
                    <div class="glass-card mb-4">
                        <h5 class="fw-bold mb-1 text-white" style="font-size: 18px;"><i class="bi bi-shield-lock-fill me-2" style="color: #ef4444;"></i>Negative Account Logouts</h5>
                        <p class="text-secondary small mb-3" style="font-size: 13px;">Zero-Hedge forced closures</p>

                        <% if(negativeLogouts && negativeLogouts.length > 0) { %>
                            <div style="max-height: 250px; overflow-y: auto;">
                                <% negativeLogouts.forEach(acc => { %>
                                    <div class="mini-row">
                                        <div>
                                            <div class="fw-bold text-white" style="font-size: 14px;"><%= acc.username %></div>
                                            <small style="color: #64748b; font-size: 12px;"><%= acc.tier %> · $<%= acc.startingBalance.toLocaleString() %></small>
                                        </div>
                                        <span class="status-badge bg-det-red">Locked</span>
                                    </div>
                                <% }) %>
                            </div>
                        <% } else { %>
                            <div class="empty-state" style="padding: 24px;">
                                <i class="bi bi-shield-check"></i>
                                <strong>No Forced Closures</strong>
                                <span>No participant has triggered a Zero-Hedge lock yet.</span>
                            </div>
                        <% } %>

                        <button class="btn btn-warning btn-sm w-100 mt-3 fw-semibold" data-bs-toggle="modal" data-bs-target="#negativeOfferModal" style="border-radius: 10px; font-size: 14px;">
                            <i class="bi bi-gift-fill me-1"></i> Issue Free Week Pass
                        </button>
                    </div>

                    <div class="glass-card">
                        <h5 class="fw-bold mb-1 text-white" style="font-size: 18px;"><i class="bi bi-graph-up-arrow me-2" style="color: #10b981;"></i>Positive Logouts</h5>
                        <p class="text-secondary small mb-3" style="font-size: 13px;">Profitable completions (100% gain)</p>

                        <% if(positiveLogouts && positiveLogouts.length > 0) { %>
                            <div style="max-height: 250px; overflow-y: auto;">
                                <% positiveLogouts.forEach(acc => { %>
                                    <div class="mini-row">
                                        <div>
                                            <div class="fw-bold text-white" style="font-size: 14px;"><%= acc.username %></div>
                                            <small style="color: #64748b; font-size: 12px;"><%= acc.tier %> · +$<%= acc.profit.toLocaleString() %></small>
                                        </div>
                                        <span class="status-badge" style="background: rgba(16,185,129,0.12); color: #10b981 !important; border: 1px solid rgba(16,185,129,0.25);">
                                            +$<%= acc.profit.toLocaleString() %>
                                        </span>
                                    </div>
                                <% }) %>
                            </div>
                        <% } else { %>
                            <div class="empty-state" style="padding: 24px;">
                                <i class="bi bi-emoji-smile"></i>
                                <strong>No Profitable Completions Yet</strong>
                                <span>Accounts that successfully double will appear here.</span>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>

            <!-- TOP EARNERS BY TIER (ACCORDION) -->
            <div id="top-earners"></div>
            <div class="row g-4 mb-4">
                <div class="col-xl-6">
                    <div class="glass-card">
                        <h5 class="fw-bold mb-3 text-white" style="font-size: 18px;"><i class="bi bi-trophy-fill me-2" style="color: #f59e0b;"></i>Top Earners by Tier</h5>
                        <p class="text-secondary small mb-3" style="font-size: 13px;">Highest verified revenue per licensed account</p>
                        
                        <div class="accordion" id="earnersAccordion">
                            <% if(tierList && tierList.length > 0) { %>
                                <% tierList.forEach((tier, idx) => { 
                                    const earners = topEarnersByTier[tier] || [];
                                %>
                                    <div class="accordion-item" style="background: transparent; border: 1px solid var(--det-border); margin-bottom: 8px; border-radius: 10px;">
                                        <h2 class="accordion-header">
                                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#earn<%= idx %>" style="font-size: 15px;">
                                                <span><%= tier %></span>
                                                <span class="badge ms-2" style="background: rgba(176,191,150,0.15); color: var(--det-green); font-size: 12px;"><%= earners.length %> earners</span>
                                            </button>
                                        </h2>
                                        <div id="earn<%= idx %>" class="accordion-collapse collapse" data-bs-parent="#earnersAccordion">
                                            <div class="accordion-body">
                                                <% if(earners.length > 0) { %>
                                                    <% earners.slice(0, 10).forEach((e, i) => { %>
                                                        <div class="mini-row">
                                                            <div class="d-flex align-items-center gap-3">
                                                                <span class="rank-badge <%= i < 3 ? 'rank-' + (i+1) : '' %>">#<%= i + 1 %></span>
                                                                <div>
                                                                    <div class="fw-bold text-white" style="font-size: 14px;"><%= e.username %></div>
                                                                    <small style="color: #64748b; font-size: 12px;"><%= e.country || 'N/A' %></small>
                                                                </div>
                                                            </div>
                                                            <span class="fw-bold" style="color: var(--det-green); font-size: 14px;">$<%= e.revenue.toLocaleString() %></span>
                                                        </div>
                                                    <% }) %>
                                                    <% if(earners.length > 10) { %>
                                                        <button class="btn btn-sm btn-outline-secondary w-100 mt-2" onclick="showTierModal('<%= tier %>', 'earners')" style="font-size: 13px;">View All <%= earners.length %> Earners</button>
                                                    <% } %>
                                                <% } else { %>
                                                    <p class="text-muted mb-0" style="font-size: 14px;">No earners in this tier yet.</p>
                                                <% } %>
                                            </div>
                                        </div>
                                    </div>
                                <% }) %>
                            <% } %>
                        </div>
                    </div>
                </div>

                <!-- TOP AFFILIATES BY TIER (ACCORDION) -->
                <div class="col-xl-6" id="affiliates">
                    <div class="glass-card">
                        <div class="d-flex justify-content-between align-items-start mb-1">
                            <h5 class="fw-bold m-0 text-white" style="font-size: 18px;"><i class="bi bi-diagram-3-fill me-2" style="color: var(--det-purple);"></i>Top Affiliates by Tier</h5>
                            <span class="badge badge-beta" style="font-size: 11px;">ENGINE v2</span>
                        </div>
                        <p class="text-secondary small mb-3" style="font-size: 13px;">Referral performance & total payouts</p>
                        
                        <div class="accordion" id="affiliatesAccordion">
                            <% if(tierList && tierList.length > 0) { %>
                                <% tierList.forEach((tier, idx) => { 
                                    const affiliates = topAffiliatesByTier[tier] || [];
                                %>
                                    <div class="accordion-item" style="background: transparent; border: 1px solid var(--det-border); margin-bottom: 8px; border-radius: 10px;">
                                        <h2 class="accordion-header">
                                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#aff<%= idx %>" style="font-size: 15px;">
                                                <span><%= tier %></span>
                                                <span class="badge ms-2" style="background: rgba(167,139,250,0.15); color: #a78bfa; font-size: 12px;"><%= affiliates.length %> affiliates</span>
                                            </button>
                                        </h2>
                                        <div id="aff<%= idx %>" class="accordion-collapse collapse" data-bs-parent="#affiliatesAccordion">
                                            <div class="accordion-body">
                                                <% if(affiliates.length > 0) { %>
                                                    <% affiliates.slice(0, 10).forEach((a, i) => { %>
                                                        <div class="mini-row">
                                                            <div class="d-flex align-items-center gap-3">
                                                                <span class="rank-badge <%= i < 3 ? 'rank-' + (i+1) : '' %>">#<%= i + 1 %></span>
                                                                <div>
                                                                    <div class="fw-bold text-white" style="font-size: 14px;"><%= a.username %></div>
                                                                    <small style="color: #64748b; font-size: 12px;"><%= a.referrals %> referrals</small>
                                                                </div>
                                                            </div>
                                                            <span class="fw-bold" style="color: var(--det-green); font-size: 14px;">$<%= a.commission.toLocaleString() %></span>
                                                        </div>
                                                    <% }) %>
                                                    <% if(affiliates.length > 10) { %>
                                                        <button class="btn btn-sm btn-outline-secondary w-100 mt-2" onclick="showTierModal('<%= tier %>', 'affiliates')" style="font-size: 13px;">View All <%= affiliates.length %> Affiliates</button>
                                                    <% } %>
                                                <% } else { %>
                                                    <p class="text-muted mb-0" style="font-size: 14px;">No affiliates in this tier yet.</p>
                                                <% } %>
                                            </div>
                                        </div>
                                    </div>
                                <% }) %>
                            <% } %>
                        </div>

                        <div class="mt-3 pt-3 border-top" style="border-color: var(--det-border) !important;">
                            <div class="d-flex justify-content-between align-items-center">
                                <span class="text-secondary" style="font-size: 14px;">Total Payouts:</span>
                                <span class="fw-bold" style="color: var(--det-green); font-size: 16px;">$<%= stats.totalAffiliatePayouts.toLocaleString() %></span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- PARTICIPANT LEDGER WITH PAGINATION -->
            <div class="row" id="ledger">
                <div class="col-12">
                    <div class="glass-card">
                        <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-3">
                            <div>
                                <h5 class="fw-bold m-0 text-white" style="font-size: 18px;"><i class="bi bi-database me-2" style="color: var(--det-green);"></i>Participant Ledger</h5>
                                <small style="color: #64748b; font-size: 12px;">Complete network registry</small>
                            </div>
                            <div class="d-flex align-items-center gap-3 flex-wrap">
                                <div class="search-box">
                                    <i class="bi bi-search"></i>
                                    <input type="text" id="ledgerSearch" class="search-input" placeholder="Search by username or email...">
                                </div>
                                <button id="exportCsvBtn" class="btn btn-sm" style="background: var(--det-card); border: 1px solid var(--det-border); color: #94a3b8; border-radius: 10px; padding: 10px 16px; font-weight: 500; font-size: 14px; transition: all var(--transition-speed);">
                                    <i class="bi bi-download me-1"></i> Export CSV
                                </button>
                            </div>
                        </div>

                        <div class="table-container">
                            <div class="table-responsive">
                                <table class="table align-middle mb-0" id="participantTable">
                                    <thead>
                                        <tr>
                                            <th>Participant</th>
                                            <th>Status</th>
                                            <th>Terminal</th>
                                            <th>Integration ID</th>
                                            <th>D.E.T Plan</th>
                                            <th>Cycle Expires</th>
                                            <th class="text-end">Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody id="ledgerTableBody">
                                        <% users.forEach(client => { %>
                                            <%
                                                let isTerminalActive = false;
                                                if (client.licenseKey && !client.isSuspended && !client.accountLocked) {
                                                    const now = new Date();
                                                    const expiry = new Date(client.licenseExpiry);
                                                    if (expiry > now) isTerminalActive = true;
                                                }
                                                
                                                let statusClass = 'bg-det-slate';
                                                let statusText = 'UNLICENSED';
                                                if(client.accountLocked) { statusClass = 'bg-det-red'; statusText = 'ZERO-HEDGE LOCK'; }
                                                else if(client.isSuspended) { statusClass = 'bg-det-warn'; statusText = 'BANNED'; }
                                                else if(new Date() > new Date(client.licenseExpiry) && client.licenseKey) { statusClass = 'bg-det-slate'; statusText = 'EXPIRED'; }
                                                else if(client.licenseKey) { statusClass = 'bg-det-green'; statusText = 'ACTIVE'; }
                                            %>
                                            <tr data-search="<%= (client.username + ' ' + (client.email || '')).toLowerCase() %>" data-status="<%= statusText.toLowerCase() %>">
                                                <td>
                                                    <div class="fw-bold text-white" style="font-size: 15px;"><%= client.username %></div>
                                                    <small style="color: #64748b; font-size: 13px;"><%= client.email || 'N/A' %></small>
                                                </td>
                                                <td>
                                                    <span class="status-badge <%= statusClass %>">
                                                        <span class="status-dot" style="background: currentColor;"></span><%= statusText %>
                                                    </span>
                                                </td>
                                                <td>
                                                    <% if(isTerminalActive) { %>
                                                        <span class="terminal-active"><i class="bi bi-circle-fill me-1" style="font-size: 6px;"></i> Active</span>
                                                    <% } else { %>
                                                        <span class="terminal-inactive"><i class="bi bi-circle me-1" style="font-size: 6px;"></i> Offline</span>
                                                    <% } %>
                                                </td>
                                                <td><span class="code-pill"><%= client.licenseKey || '---' %></span></td>
                                                <td><span class="badge border text-white" style="background: #0a0a0a; border-color: var(--det-border) !important; padding: 6px 12px; font-size: 13px; font-weight: 500; border-radius: 8px;"><%= client.currentTier || 'None' %></span></td>
                                                <td class="fw-semibold <%= new Date() > new Date(client.licenseExpiry) && client.licenseKey ? 'text-danger' : 'text-white' %>" style="font-size: 14px;">
                                                    <%= client.licenseExpiry ? new Date(client.licenseExpiry).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) : 'N/A' %>
                                                </td>
                                                <td class="text-end">
                                                    <div class="action-group">
                                                        <button class="action-btn btn-details" title="View Details" data-bs-toggle="modal" data-bs-target="#detailModal<%= client._id %>">
                                                            <i class="bi bi-info-circle-fill"></i>
                                                        </button>
                                                        <button class="action-btn btn-manage" title="Manage License" data-bs-toggle="modal" data-bs-target="#manageModal<%= client._id %>">
                                                            <i class="bi bi-key-fill"></i>
                                                        </button>
                                                        <% if(client.licenseKey) { %>
                                                            <form action="/admin/suspend-license/<%= client._id %>" method="POST" class="d-inline">
                                                                <button type="submit" class="action-btn btn-ban" title="<%= client.isSuspended ? 'Unban' : 'Ban' %> Account">
                                                                    <i class="bi <%= client.isSuspended ? 'bi-shield-check' : 'bi-shield-lock-fill' %>"></i>
                                                                </button>
                                                            </form>
                                                        <% } %>
                                                        <button class="action-btn btn-delete" title="Delete" data-bs-toggle="modal" data-bs-target="#deleteModal<%= client._id %>">
                                                            <i class="bi bi-trash3-fill"></i>
                                                        </button>
                                                    </div>
                                                </td>
                                            </tr>

                                            <!-- Detail Modal -->
                                            <div class="modal fade" id="detailModal<%= client._id %>" tabindex="-1" aria-hidden="true">
                                                <div class="modal-dialog modal-dialog-centered modal-lg">
                                                    <div class="modal-content">
                                                        <div class="modal-header">
                                                            <h5 class="modal-title fw-bold text-white" style="font-size: 18px;"><i class="bi bi-person-badge me-2"></i><%= client.username %> — Dossier</h5>
                                                            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                                                        </div>
                                                        <div class="modal-body">
                                                            <div class="row g-4">
                                                                <div class="col-md-6">
                                                                    <h6 class="fw-bold text-white mb-3" style="font-size: 13px; text-transform: uppercase; letter-spacing: 1.5px; color: #64748b;">Account Information</h6>
                                                                    <div class="detail-row"><span class="detail-label">Username</span><span class="detail-value"><%= client.username %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">Email</span><span class="detail-value"><%= client.email || 'N/A' %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">WhatsApp</span><span class="detail-value"><%= client.whatsapp || 'N/A' %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">Country</span><span class="detail-value"><%= (client.country && client.country.trim()) || 'Unknown' %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">D.E.T Plan</span><span class="detail-value"><%= client.currentTier || 'None' %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">Integration ID</span><span class="detail-value"><%= client.licenseKey || 'Not Issued' %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">License Expiry</span><span class="detail-value <%= new Date() > new Date(client.licenseExpiry) && client.licenseKey ? 'text-danger' : '' %>"><%= client.licenseExpiry ? new Date(client.licenseExpiry).toLocaleDateString() : 'N/A' %></span></div>
                                                                </div>
                                                                <div class="col-md-6">
                                                                    <h6 class="fw-bold text-white mb-3" style="font-size: 13px; text-transform: uppercase; letter-spacing: 1.5px; color: #64748b;">Equity & Trading</h6>
                                                                    <div class="detail-row"><span class="detail-label">Starting Balance</span><span class="detail-value">$<%= (Number(client.startingBalance) || 0).toLocaleString() %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">Target Balance</span><span class="detail-value">$<%= (Number(client.targetBalance) || 0).toLocaleString() %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">Prepayment</span><span class="detail-value">$<%= (Number(client.prepaymentAmount) || 0).toLocaleString() %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">Account Locked</span><span class="detail-value <%= client.accountLocked ? 'text-danger' : 'text-success' %>"><%= client.accountLocked ? 'YES' : 'No' %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">Suspended</span><span class="detail-value <%= client.isSuspended ? 'text-warning' : 'text-success' %>"><%= client.isSuspended ? 'BANNED' : 'Clear' %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">Terminal</span><span class="detail-value <%= isTerminalActive ? 'text-success' : '' %>"><%= isTerminalActive ? 'Active' : 'Offline' %></span></div>
                                                                    <div class="detail-row"><span class="detail-label">Registered</span><span class="detail-value"><%= client._id.getTimestamp().toLocaleDateString() %></span></div>
                                                                </div>
                                                            </div>
                                                        </div>
                                                        <div class="modal-footer">
                                                            <button type="button" class="btn btn-outline-light" data-bs-dismiss="modal" style="border-radius: 10px;">Close</button>
                                                            <button type="button" class="btn-det" data-bs-toggle="modal" data-bs-target="#manageModal<%= client._id %>" data-bs-dismiss="modal">
                                                                <i class="bi bi-key-fill me-1"></i> Manage License
                                                            </button>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>

                                            <!-- Manage License Modal -->
                                            <div class="modal fade" id="manageModal<%= client._id %>" tabindex="-1" aria-hidden="true">
                                                <div class="modal-dialog modal-dialog-centered">
                                                    <div class="modal-content">
                                                        <div class="modal-header">
                                                            <h5 class="modal-title fw-bold text-white" style="font-size: 18px;">Manage D.E.T Access</h5>
                                                            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                                                        </div>
                                                        <form action="/admin/generate-license/<%= client._id %>" method="POST">
                                                            <div class="modal-body">
                                                                <p style="color: #94a3b8; font-size: 14px; margin-bottom: 20px;">Generating a new key for <strong class="text-white"><%= client.username %></strong>.</p>
                                                                <div class="mb-3">
                                                                    <label class="form-label fw-bold small" style="color: var(--det-green) !important; text-transform: uppercase; letter-spacing: 1px; font-size: 13px;">Trading Level</label>
                                                                    <select class="form-select" name="tierLevel" required>
                                                                        <% ['Amber', 'Amethyst', 'Topaz', 'Tanzanite', 'Sapphire', 'Emerald'].forEach(tier => { %>
                                                                            <option value="<%= tier %>" <%= client.currentTier == tier ? 'selected' : '' %>><%= tier %></option>
                                                                        <% }) %>
                                                                    </select>
                                                                </div>
                                                                <div class="mb-3">
                                                                    <label class="form-label fw-bold small" style="color: var(--det-green) !important; text-transform: uppercase; letter-spacing: 1px; font-size: 13px;">Duration (Days)</label>
                                                                    <input type="number" name="durationDays" class="form-control" value="30" required>
                                                                </div>
                                                            </div>
                                                            <div class="modal-footer">
                                                                <button type="button" class="btn btn-outline-light" data-bs-dismiss="modal" style="border-radius: 10px;">Cancel</button>
                                                                <button type="submit" class="btn-det"><i class="bi bi-lightning-charge-fill me-1"></i> Issue Key</button>
                                                            </div>
                                                        </form>
                                                    </div>
                                                </div>
                                            </div>

                                            <!-- Delete Modal -->
                                            <div class="modal fade" id="deleteModal<%= client._id %>" tabindex="-1" aria-hidden="true">
                                                <div class="modal-dialog modal-dialog-centered">
                                                    <div class="modal-content" style="border-color: rgba(239,68,68,0.3);">
                                                        <div class="modal-header" style="border-bottom-color: rgba(239,68,68,0.1);">
                                                            <h5 class="modal-title fw-bold text-danger" style="font-size: 18px;"><i class="bi bi-exclamation-triangle-fill me-2"></i>Confirm Deletion</h5>
                                                            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                                                        </div>
                                                        <div class="modal-body">
                                                            <p style="color: #94a3b8; font-size: 15px;">Delete <strong class="text-white"><%= client.username %></strong> permanently?</p>
                                                            <p class="text-danger small mb-0 fw-bold" style="font-size: 14px;">This is irreversible. Their EA will disconnect immediately.</p>
                                                        </div>
                                                        <div class="modal-footer" style="border-top-color: rgba(239,68,68,0.1);">
                                                            <button type="button" class="btn btn-outline-light" data-bs-dismiss="modal" style="border-radius: 10px;">Cancel</button>
                                                            <form action="/admin/delete-user/<%= client._id %>" method="POST">
                                                                <button type="submit" class="btn btn-danger" style="border-radius: 10px; font-size: 15px;">Nuke Account</button>
                                                            </form>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        <% }) %>
                                    </tbody>
                                </table>
                            </div>
                            
                            <!-- Pagination -->
                            <div class="pagination-container" id="paginationContainer">
                                <div class="pagination-info">
                                    Showing <span id="showingStart">0</span>-<span id="showingEnd">0</span> of <span id="totalRecords">0</span> participants
                                </div>
                                <div class="pagination-controls">
                                    <select class="page-size-select" id="pageSizeSelect">
                                        <option value="10">10 per page</option>
                                        <option value="25">25 per page</option>
                                        <option value="50">50 per page</option>
                                        <option value="100">100 per page</option>
                                    </select>
                                    <div class="pagination" id="pagination"></div>
                                </div>
                            </div>
                        </div>
                        
                        <div id="noResultsMsg" class="empty-state" style="display: none;">
                            <i class="bi bi-search"></i>
                            <strong>No matches found</strong>
                            <span>Try a different search term.</span>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </main>

    <!-- Negative Account Offer Modal -->
    <div class="modal fade" id="negativeOfferModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title fw-bold text-white" style="font-size: 18px;"><i class="bi bi-gift-fill me-2" style="color: var(--det-amber);"></i>Negative Account Offer Service</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <p style="color: #94a3b8; font-size: 14px; margin-bottom: 24px;">Accounts locked by Zero-Hedge protocol. Grant 1-week free access to continue trading.</p>
                    
                    <% 
                    let blownAccounts = users.filter(c => c.accountLocked);
                    if(blownAccounts.length > 0) { 
                    %>
                        <div class="table-responsive">
                            <table class="table align-middle mb-0">
                                <thead style="background: #0a0a0a;">
                                    <tr>
                                        <th style="color: #64748b; font-size: 11px; text-transform: uppercase; letter-spacing: 1px;">Participant</th>
                                        <th style="color: #64748b; font-size: 11px; text-transform: uppercase; letter-spacing: 1px;">Tier</th>
                                        <th style="color: #64748b; font-size: 11px; text-transform: uppercase; letter-spacing: 1px;">Balance Range</th>
                                        <th style="color: #64748b; font-size: 11px; text-transform: uppercase; letter-spacing: 1px;">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% blownAccounts.forEach(acc => { %>
                                        <tr>
                                            <td>
                                                <div class="fw-bold text-white" style="font-size: 15px;"><%= acc.username %></div>
                                                <small style="color: #64748b; font-size: 13px;"><%= acc.email %></small>
                                            </td>
                                            <td><span class="badge border text-white" style="background: #0a0a0a; border-color: var(--det-border) !important; font-size: 13px;"><%= acc.currentTier || 'None' %></span></td>
                                            <td style="color: #e2e8f0; font-size: 14px;">$<%= (Number(acc.startingBalance) || 0).toLocaleString() %> → $<%= (Number(acc.targetBalance) || 0).toLocaleString() %></td>
                                            <td>
                                                <form action="/admin/offer-free-week/<%= acc._id %>" method="POST">
                                                    <button type="submit" class="btn btn-warning btn-sm fw-semibold" style="border-radius: 8px; font-size: 13px;">
                                                        <i class="bi bi-gift me-1"></i> Grant Free Week
                                                    </button>
                                                </form>
                                            </td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                    <% } else { %>
                        <div class="empty-state">
                            <i class="bi bi-shield-check"></i>
                            <strong>No Blown Accounts</strong>
                            <span>Locked accounts will appear here automatically.</span>
                        </div>
                    <% } %>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-light" data-bs-dismiss="modal" style="border-radius: 10px; font-size: 14px;">Close</button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // ==========================================
        // SAFE DATA PARSING
        // ==========================================
        const registrationTrend = <%- JSON.stringify(registrationTrend || []) %>;
        const revenueByTier = <%- JSON.stringify(revenueByTier || []) %>;
        const topAccountsByRegion = <%- JSON.stringify(topAccountsByRegion || []) %>;
        const statusBreakdown = [
            { label: 'Active', value: <%= stats.active || 0 %>, color: '#B0BF96' },
            { label: 'Banned', value: <%= stats.suspended || 0 %>, color: '#f59e0b' },
            { label: 'Locked', value: <%= stats.locked || 0 %>, color: '#ef4444' },
            { label: 'Expired', value: <%= stats.expired || 0 %>, color: '#94a3b8' },
            { label: 'Unlicensed', value: <%= stats.unlicensed || 0 %>, color: '#38bdf8' }
        ];
        const tierList = <%- JSON.stringify(tierList || []) %>;
        const topEarnersByTier = <%- JSON.stringify(topEarnersByTier || {}) %>;
        const topAffiliatesByTier = <%- JSON.stringify(topAffiliatesByTier || {}) %>;

        // ==========================================
        // THEME TOGGLE
        // ==========================================
        let currentTheme = 'dark';
        const themeToggle = document.getElementById('themeToggle');
        const themeIcon = document.getElementById('themeIcon');
        
        function setTheme(theme) {
            currentTheme = theme;
            document.documentElement.setAttribute('data-bs-theme', theme);
            themeIcon.className = theme === 'dark' ? 'bi bi-sun-fill' : 'bi bi-moon-fill';
            if (window.equityChart) { window.equityChart.destroy(); initEquityChart(); }
        }
        
        themeToggle?.addEventListener('click', () => setTheme(currentTheme === 'dark' ? 'light' : 'dark'));

        // ==========================================
        // SIDEBAR TOGGLE
        // ==========================================
        const sidebar = document.getElementById('sidebar');
        const sidebarToggle = document.getElementById('sidebarToggle');
        const backdrop = document.createElement('div');
        backdrop.className = 'sidebar-backdrop';
        document.body.appendChild(backdrop);

        function openSidebar() { sidebar.classList.add('show'); backdrop.classList.add('show'); }
        function closeSidebar() { sidebar.classList.remove('show'); backdrop.classList.remove('show'); }
        sidebarToggle.addEventListener('click', () => sidebar.classList.contains('show') ? closeSidebar() : openSidebar());
        backdrop.addEventListener('click', closeSidebar);
        document.querySelectorAll('.sidebar .nav-link').forEach(link => link.addEventListener('click', closeSidebar));

        // ==========================================
        // CHARTS INITIALIZATION
        // ==========================================
        document.addEventListener('DOMContentLoaded', function() {
            const regCtx = document.getElementById('registrationChart');
            if (regCtx && registrationTrend.length > 0) {
                new Chart(regCtx.getContext('2d'), {
                    type: 'line',
                    data: {
                        labels: registrationTrend.map(d => {
                            try { return new Date(d.date + 'T00:00:00').toLocaleDateString('en-US', { month: 'short', day: 'numeric' }); }
                            catch(e) { return d.date || ''; }
                        }),
                        datasets: [{
                            label: 'New Accounts',
                            data: registrationTrend.map(d => d.count || 0),
                            borderColor: '#B0BF96',
                            backgroundColor: 'rgba(176, 191, 150, 0.1)',
                            borderWidth: 2.5,
                            fill: true,
                            tension: 0.4,
                            pointRadius: 0,
                            pointHoverRadius: 6,
                            pointHoverBackgroundColor: '#B0BF96'
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        interaction: { intersect: false, mode: 'index' },
                        scales: {
                            x: { grid: { display: false }, ticks: { maxRotation: 0, autoSkip: true, maxTicksLimit: 10, font: { size: 12 } } },
                            y: { beginAtZero: true, grid: { color: 'rgba(255,255,255,0.03)' }, ticks: { precision: 0, font: { size: 12 } } }
                        },
                        plugins: { legend: { display: false } }
                    }
                });
            }

            const statusCtx = document.getElementById('statusChart');
            if (statusCtx) {
                new Chart(statusCtx.getContext('2d'), {
                    type: 'doughnut',
                    data: {
                        labels: statusBreakdown.map(d => d.label),
                        datasets: [{ data: statusBreakdown.map(d => d.value), backgroundColor: statusBreakdown.map(d => d.color), borderColor: '#141414', borderWidth: 4 }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        cutout: '70%',
                        plugins: { legend: { display: false } }
                    }
                });
            }

            const revCtx = document.getElementById('revenueChart');
            if (revCtx && revenueByTier.length > 0) {
                const colors = ['rgba(176,191,150,0.9)', 'rgba(56,189,248,0.8)', 'rgba(245,158,11,0.8)', 'rgba(167,139,250,0.8)', 'rgba(16,185,129,0.8)', 'rgba(239,68,68,0.7)'];
                new Chart(revCtx.getContext('2d'), {
                    type: 'bar',
                    data: {
                        labels: revenueByTier.map(t => t.tier),
                        datasets: [{ data: revenueByTier.map(t => t.revenue), backgroundColor: revenueByTier.map((_, i) => colors[i % colors.length]), borderRadius: 8, maxBarThickness: 48 }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        scales: { x: { grid: { display: false }, ticks: { font: { size: 11 } } }, y: { beginAtZero: true, grid: { color: 'rgba(255,255,255,0.03)' }, ticks: { callback: v => '$' + v.toLocaleString(), font: { size: 12 } } } },
                        plugins: { legend: { display: false } }
                    }
                });
            }

            const regionCtx = document.getElementById('regionChart');
            if (regionCtx && topAccountsByRegion.length > 0) {
                new Chart(regionCtx.getContext('2d'), {
                    type: 'bar',
                    data: {
                        labels: topAccountsByRegion.map(r => r.region),
                        datasets: [{ data: topAccountsByRegion.map(r => r.count), backgroundColor: ['#B0BF96', '#38bdf8', '#f59e0b', '#a78bfa', '#94a3b8', '#10b981', '#ef4444'], borderRadius: 8, maxBarThickness: 32 }]
                    },
                    options: {
                        indexAxis: 'y',
                        responsive: true,
                        maintainAspectRatio: false,
                        scales: { x: { beginAtZero: true, grid: { color: 'rgba(255,255,255,0.03)' }, ticks: { precision: 0, font: { size: 12 } } }, y: { grid: { display: false }, ticks: { font: { size: 12 } } } },
                        plugins: { legend: { display: false } }
                    }
                });
            }

            initEquityChart();
        });

        // ==========================================
        // LIVE EQUITY CHART WITH PERIOD SELECTOR
        // ==========================================
        function initEquityChart() {
            const ctx = document.getElementById('equityChart');
            if (!ctx) return;
            
            const periodMap = { '1h': 60, '1d': 144, '1w': 168, '1M': 200, '1y': 200 };
            const period = document.getElementById('equityPeriod')?.value || '1d';
            const points = periodMap[period] || 144;
            
            window.equityChart = new Chart(ctx.getContext('2d'), {
                type: 'line',
                data: { 
                    labels: Array(points).fill(''), 
                    datasets: [{ 
                        label: 'Master Equity', 
                        data: Array(points).fill(0), 
                        borderColor: '#B0BF96', 
                        backgroundColor: 'rgba(176, 191, 150, 0.1)',
                        borderWidth: 2, 
                        fill: true, 
                        tension: 0.3, 
                        pointRadius: 0 
                    }] 
                },
                options: { 
                    responsive: true, 
                    maintainAspectRatio: false,
                    interaction: { intersect: false, mode: 'index' },
                    scales: { 
                        x: { display: false }, 
                        y: { display: true, grid: { color: 'rgba(255,255,255,0.03)' }, ticks: { color: '#94a3b8', font: { size: 11 } } } 
                    }, 
                    plugins: { legend: { display: false } }, 
                    animation: { duration: 0 } 
                }
            });
        }

        document.getElementById('equityPeriod')?.addEventListener('change', () => {
            if (window.equityChart) window.equityChart.destroy();
            initEquityChart();
        });

        // ==========================================
        // TIER MODAL FUNCTION
        // ==========================================
        window.showTierModal = function(tier, type) {
            const data = type === 'earners' ? (topEarnersByTier[tier] || []) : (topAffiliatesByTier[tier] || []);
            const label = type === 'earners' ? 'Revenue' : 'Commission';
            const valueKey = type === 'earners' ? 'revenue' : 'commission';
            
            document.getElementById('tierDetailModal')?.remove();
            
            const modalHtml = `
                <div class="modal fade" id="tierDetailModal" tabindex="-1" aria-hidden="true">
                    <div class="modal-dialog modal-lg modal-dialog-scrollable modal-dialog-centered">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title fw-bold text-white" style="font-size: 18px;">${tier} — ${type === 'earners' ? 'Top Earners' : 'Top Affiliates'}</h5>
                                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                            </div>
                            <div class="modal-body">
                                <div class="table-responsive">
                                    <table class="table table-sm">
                                        <thead><tr><th>#</th><th>User</th><th class="text-end">${label}</th></tr></thead>
                                        <tbody id="tierModalBody">
                                            ${data.map((item, i) => `
                                                <tr class="tier-row">
                                                    <td style="font-size: 15px;">${i + 1}</td>
                                                    <td><strong style="font-size: 15px;">${item.username}</strong></td>
                                                    <td class="text-end fw-bold" style="color: var(--det-green); font-size: 15px;">$${item[valueKey].toLocaleString()}</td>
                                                </tr>
                                            `).join('')}
                                            ${data.length === 0 ? '<tr><td colspan="3" class="text-center text-muted" style="font-size: 15px;">No entries found</td></tr>' : ''}
                                        </tbody>
                                    </table>
                                </div>
                                <nav class="mt-3 d-flex justify-content-between align-items-center" id="tierPagination">
                                    <small class="text-muted" id="tierPageInfo" style="font-size: 14px;"></small>
                                    <div class="d-flex gap-1" id="tierPageButtons"></div>
                                </nav>
                            </div>
                        </div>
                    </div>
                </div>`;
            
            document.body.insertAdjacentHTML('beforeend', modalHtml);
            const modal = new bootstrap.Modal(document.getElementById('tierDetailModal'));
            modal.show();
            
            setTimeout(() => {
                const rows = Array.from(document.querySelectorAll('#tierModalBody .tier-row'));
                if (rows.length === 0) return;
                
                const pageSize = 15;
                let currentPage = 1;
                const totalPages = Math.ceil(rows.length / pageSize);
                
                function updatePagination() {
                    rows.forEach((row, i) => {
                        row.style.display = (i >= (currentPage - 1) * pageSize && i < currentPage * pageSize) ? '' : 'none';
                    });
                    
                    const start = (currentPage - 1) * pageSize + 1;
                    const end = Math.min(currentPage * pageSize, rows.length);
                    document.getElementById('tierPageInfo').textContent = `Showing ${start}-${end} of ${rows.length}`;
                    
                    let btns = '';
                    btns += `<button class="page-btn" ${currentPage === 1 ? 'disabled' : ''} onclick="window.tierPaginate(${currentPage - 1})"><i class="bi bi-chevron-left"></i></button>`;
                    for (let p = 1; p <= Math.min(totalPages, 5); p++) {
                        btns += `<button class="page-btn ${p === currentPage ? 'active' : ''}" onclick="window.tierPaginate(${p})">${p}</button>`;
                    }
                    if (totalPages > 5) {
                        btns += `<button class="page-btn disabled">...</button>`;
                        btns += `<button class="page-btn ${totalPages === currentPage ? 'active' : ''}" onclick="window.tierPaginate(${totalPages})">${totalPages}</button>`;
                    }
                    btns += `<button class="page-btn" ${currentPage === totalPages ? 'disabled' : ''} onclick="window.tierPaginate(${currentPage + 1})"><i class="bi bi-chevron-right"></i></button>`;
                    document.getElementById('tierPageButtons').innerHTML = btns;
                }
                
                window.tierPaginate = function(page) { currentPage = page; updatePagination(); };
                updatePagination();
            }, 200);
            
            document.getElementById('tierDetailModal').addEventListener('hidden.bs.modal', function() { this.remove(); });
        };

        // ==========================================
        // LEDGER PAGINATION SYSTEM
        // ==========================================
        const ledgerSearchInput = document.getElementById('ledgerSearch');
        const ledgerTableBody = document.getElementById('ledgerTableBody');
        const noResultsMsg = document.getElementById('noResultsMsg');
        const pageSizeSelect = document.getElementById('pageSizeSelect');
        const paginationDiv = document.getElementById('pagination');
        const showingStart = document.getElementById('showingStart');
        const showingEnd = document.getElementById('showingEnd');
        const totalRecords = document.getElementById('totalRecords');

        let currentPage = 1;
        let pageSize = 10;
        let allRows = [];
        let filteredRows = [];

        if (ledgerTableBody) {
            allRows = Array.from(ledgerTableBody.querySelectorAll('tr[data-search]'));
            filteredRows = [...allRows];
        }

        function updatePagination() {
            const total = filteredRows.length;
            const totalPages = Math.ceil(total / pageSize);
            const start = total === 0 ? 0 : (currentPage - 1) * pageSize + 1;
            const end = Math.min(currentPage * pageSize, total);

            if (showingStart) showingStart.textContent = start;
            if (showingEnd) showingEnd.textContent = end;
            if (totalRecords) totalRecords.textContent = total;

            allRows.forEach(row => row.classList.add('row-hidden'));
            filteredRows.forEach((row, i) => {
                if (i >= (currentPage - 1) * pageSize && i < currentPage * pageSize) {
                    row.classList.remove('row-hidden');
                }
            });

            if (paginationDiv) {
                paginationDiv.innerHTML = '';
                
                const prevBtn = document.createElement('button');
                prevBtn.className = 'page-btn';
                prevBtn.innerHTML = '<i class="bi bi-chevron-left"></i>';
                prevBtn.disabled = currentPage === 1;
                prevBtn.addEventListener('click', () => { if (currentPage > 1) { currentPage--; updatePagination(); } });
                paginationDiv.appendChild(prevBtn);

                const maxVisible = 5;
                let startPage = Math.max(1, currentPage - Math.floor(maxVisible / 2));
                let endPage = Math.min(totalPages, startPage + maxVisible - 1);
                if (endPage - startPage < maxVisible - 1) startPage = Math.max(1, endPage - maxVisible + 1);

                if (startPage > 1) {
                    const firstBtn = document.createElement('button');
                    firstBtn.className = 'page-btn';
                    firstBtn.textContent = '1';
                    firstBtn.addEventListener('click', () => { currentPage = 1; updatePagination(); });
                    paginationDiv.appendChild(firstBtn);
                    if (startPage > 2) {
                        const dots = document.createElement('span');
                        dots.className = 'page-btn';
                        dots.textContent = '...';
                        dots.style.cursor = 'default';
                        dots.style.pointerEvents = 'none';
                        paginationDiv.appendChild(dots);
                    }
                }

                for (let i = startPage; i <= endPage; i++) {
                    const pageBtn = document.createElement('button');
                    pageBtn.className = 'page-btn' + (i === currentPage ? ' active' : '');
                    pageBtn.textContent = i;
                    pageBtn.addEventListener('click', () => { currentPage = i; updatePagination(); });
                    paginationDiv.appendChild(pageBtn);
                }

                if (endPage < totalPages) {
                    if (endPage < totalPages - 1) {
                        const dots = document.createElement('span');
                        dots.className = 'page-btn';
                        dots.textContent = '...';
                        dots.style.cursor = 'default';
                        dots.style.pointerEvents = 'none';
                        paginationDiv.appendChild(dots);
                    }
                    const lastBtn = document.createElement('button');
                    lastBtn.className = 'page-btn';
                    lastBtn.textContent = totalPages;
                    lastBtn.addEventListener('click', () => { currentPage = totalPages; updatePagination(); });
                    paginationDiv.appendChild(lastBtn);
                }

                const nextBtn = document.createElement('button');
                nextBtn.className = 'page-btn';
                nextBtn.innerHTML = '<i class="bi bi-chevron-right"></i>';
                nextBtn.disabled = currentPage === totalPages || totalPages === 0;
                nextBtn.addEventListener('click', () => { if (currentPage < totalPages) { currentPage++; updatePagination(); } });
                paginationDiv.appendChild(nextBtn);
            }

            if (noResultsMsg) {
                const searchTerm = ledgerSearchInput ? ledgerSearchInput.value.trim() : '';
                noResultsMsg.style.display = (filteredRows.length === 0 && searchTerm.length > 0) ? 'flex' : 'none';
            }
        }

        if (ledgerSearchInput) {
            ledgerSearchInput.addEventListener('input', function() {
                const term = this.value.trim().toLowerCase();
                filteredRows = allRows.filter(row => {
                    const searchData = row.getAttribute('data-search') || '';
                    return searchData.includes(term);
                });
                currentPage = 1;
                updatePagination();
            });
        }

        if (pageSizeSelect) {
            pageSizeSelect.addEventListener('change', function() {
                pageSize = parseInt(this.value);
                currentPage = 1;
                updatePagination();
            });
        }

        updatePagination();

        // ==========================================
        // CSV EXPORT
        // ==========================================
        const exportBtn = document.getElementById('exportCsvBtn');
        if (exportBtn) {
            exportBtn.addEventListener('click', function() {
                let csv = [];
                const table = document.getElementById('participantTable');
                if (!table) return;
                
                const rows = table.querySelectorAll('tr');
                for (let i = 0; i < rows.length; i++) {
                    if (rows[i].classList.contains('row-hidden')) continue;
                    let row = [];
                    const cols = rows[i].querySelectorAll('td, th');
                    for (let j = 0; j < cols.length - 1; j++) { 
                        let data = cols[j].innerText.replace(/(\r\n|\n|\r)/gm, " ").trim();
                        row.push('"' + data.replace(/"/g, '""') + '"');
                    }
                    csv.push(row.join(','));
                }
                
                if (csv.length === 0) return;
                
                const csvFile = new Blob([csv.join('\n')], {type: 'text/csv'});
                const downloadLink = document.createElement('a');
                downloadLink.download = 'DET_Participant_Ledger.csv';
                downloadLink.href = window.URL.createObjectURL(csvFile);
                downloadLink.style.display = 'none';
                document.body.appendChild(downloadLink);
                downloadLink.click();
                document.body.removeChild(downloadLink);
            });
        }

        // ==========================================
        // LIVE DATA POLLING
        // ==========================================
        async function fetchAdminEAData() {
            try {
                const res = await fetch('/api/public/ea-state');
                const data = await res.json();
                
                const symbolEl = document.getElementById('ea-symbol');
                const priceEl = document.getElementById('ea-price');
                const trendEl = document.getElementById('ea-trend');
                const tradesEl = document.getElementById('ea-trades');
                
                if (symbolEl) symbolEl.innerText = data.symbol || '--';
                if (priceEl) priceEl.innerText = data.price ? parseFloat(data.price).toFixed(5) : '0.00000';
                if (trendEl) {
                    trendEl.innerText = data.trend || '--';
                    if (data.action && data.action.includes('Buy')) trendEl.style.color = '#10b981';
                    else if (data.action && data.action.includes('Sell')) trendEl.style.color = '#ef4444';
                    else trendEl.style.color = '#e2e8f0';
                }
                if (tradesEl) tradesEl.innerText = data.openTrades || 0;

                if (window.equityChart && data.equityHistory && data.equityHistory.length > 0) {
                    window.equityChart.data.datasets[0].data = data.equityHistory;
                    window.equityChart.update('none');
                }
            } catch (e) {
                console.error('Live feed error:', e);
            }
        }
        setInterval(fetchAdminEAData, 2000);
        fetchAdminEAData();

        console.log('%c🛡️ D.E.T Master Node Console v2.5.0 %c| %cSecure Session Active',
            'color: #B0BF96; font-weight: bold; font-size: 15px;',
            '',
            'color: #94a3b8; font-size: 12px;');
    </script>
</body>
</html>